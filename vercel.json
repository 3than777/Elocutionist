{
  "outputDirectory": "dreamcollege-frontend/dist",
  "buildCommand": "npm run build && cd dreamcollege-frontend && npm install && npm run build",
  "installCommand": "npm install && cd dreamcollege-frontend && npm install",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 10
    }
  }
}